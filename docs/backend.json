
{
  "entities": {
    "Book": {
      "title": "Book",
      "description": "Information about an uploaded PDF book.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "classId": { "type": "string" },
        "subject": { "type": "string" },
        "chapterName": { "type": "string", "description": "Name of the specific chapter for guide books." },
        "fileName": { "type": "string" },
        "pdfUrl": { "type": "string" },
        "coverImageUrl": { "type": "string", "description": "URL to the book's cover image." },
        "isGuide": { "type": "boolean", "default": false },
        "uploadedAt": { "type": "string", "format": "date-time" },
        "userId": { "type": "string" }
      },
      "required": ["classId", "subject", "fileName", "pdfUrl"]
    },
    "QuestionSet": {
      "title": "Question Set",
      "description": "A collection of questions for an exam.",
      "type": "object",
      "properties": {
        "institution": { "type": "string" },
        "exam": { "type": "string" },
        "classId": { "type": "string" },
        "subject": { "type": "string" },
        "time": { "type": "string" },
        "totalMarks": { "type": "string" },
        "isMcq": { "type": "boolean" },
        "marksA": { "type": "number" },
        "marksB": { "type": "number" },
        "marksC": { "type": "number" },
        "marksD": { "type": "number" },
        "creativeInstruction": { "type": "string" },
        "shortInstruction": { "type": "string" },
        "questions": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "type": { "type": "string", "enum": ["creative", "short", "mcq"] },
              "stimulus": { "type": "string" },
              "qA": { "type": "string" },
              "qB": { "type": "string" },
              "qC": { "type": "string" },
              "qD": { "type": "string" },
              "shortText": { "type": "string" },
              "mcqQuestion": { "type": "string" },
              "optA": { "type": "string" },
              "optB": { "type": "string" },
              "optC": { "type": "string" },
              "optD": { "type": "string" },
              "correctOpt": { "type": "string" }
            }
          }
        },
        "createdAt": { "type": "string", "format": "date-time" },
        "updatedAt": { "type": "string", "format": "date-time" },
        "userId": { "type": "string" }
      }
    },
    "AdminConfig": {
      "title": "Admin Config",
      "description": "Stores the UID of the first registered admin.",
      "type": "object",
      "properties": {
        "adminUid": { "type": "string" }
      }
    },
    "SoftwareConfig": {
      "title": "Software Config",
      "description": "Global settings for the application branding.",
      "type": "object",
      "properties": {
        "appName": { "type": "string" },
        "appLogoUrl": { "type": "string" }
      }
    }
  },
  "auth": {
    "providers": ["password"]
  },
  "firestore": {
    "/books/{bookId}": {
      "schema": "Book",
      "description": "Collection of uploaded books metadata."
    },
    "/questions/{questionId}": {
      "schema": "QuestionSet",
      "description": "Stored question sets created by users."
    },
    "/config/admin": {
      "schema": "AdminConfig",
      "description": "Global config to store the first admin's UID."
    },
    "/config/software": {
      "schema": "SoftwareConfig",
      "description": "Global software settings branding."
    }
  }
}
